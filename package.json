{
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --headless",
    "cypress:open": "npx cypress open",
    "cypress:runDefault": "npx cypress run --spec cypress/e2e/*.spec.cy.js",
    "cypress:chrome": "npx cypress run --browser chrome --spec cypress/e2e/*.spec.cy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^13.15.0",
    "eslint": "^9.14.0",
    "eslint-plugin-cypress": "^4.1.0"
  }
}
